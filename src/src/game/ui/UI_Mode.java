package src.game.ui;

/**
 * UI_Mode is an enumeration listing every possibilities of interface between the player and the game.
 * The available modes are :
 * <li>{@link #CONSOLE}</li>
 * 
 * @author alexandre
 * @version 0.1
 */

public enum UI_Mode {
	/**
	 * The CONSOLE mode allow the game and the user to communicate using a terminal.
	 */
	CONSOLE,
}
